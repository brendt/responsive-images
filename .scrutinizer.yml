build:
    environment:
        php:
            version: 5.6
    tests:
        override:
            -
                command: 'phpunit'
checks:
    php:
        code_rating: true
        duplication: true
